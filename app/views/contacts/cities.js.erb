<% if @cities.present?  %>
	if($('#contact_work_city').length > 0){
    $('#contact_work_city').html("<%= escape_javascript options_for_select(@cities) %>");
   }
  else{
    $('#artist_profile_location2').html("<%= escape_javascript options_for_select(@cities) %>");
  }
<% end %>



