<% if @states.present?  %>
	if($('#contact_work_state').length > 0){
    $('#contact_work_state').html("<%= escape_javascript options_for_select(@states) %>");
   }
  else{
    $('#artist_profile_state').html("<%= escape_javascript options_for_select(@states) %>");
  }
<% end %>

