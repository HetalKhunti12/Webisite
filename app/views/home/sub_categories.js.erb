<% if @sub_categories.present?  %>
  $('#sub_categories').html("<%= escape_javascript options_for_select(@sub_categories) %>");
  $('#sub_categories').prepend($("<option></option>")
                    .attr("value","")
                    .text("Select Sub Category"));
  $('#sub_categories option[value=""]').attr('selected','selected');
<% end %>